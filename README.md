# DevOps Essentials Assignment

This is a simple Python + Flask web application demonstrating core DevOps skills.

## Features
- Flask app with Redis backend
- Dockerized and orchestrated with Docker Compose
- Health check endpoint for CI validation
- GitHub Actions workflow to test app

## Usage

### Build and run locally
```bash
docker-compose up --build
